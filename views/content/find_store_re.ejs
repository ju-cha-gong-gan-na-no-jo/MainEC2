<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/main.css"/>
    <link rel="stylesheet" href="/css/pu.css"/>

    <style>
      ::-webkit-scrollbar{width: 8px; height: 8px;}
      ::-webkit-scrollbar-track{background-color: #ffffff;}
      ::-webkit-scrollbar-thumb{background-color: #D3D3D3; border-radius: 7px;}
    </style>
</head>
<body>
  <div class="x18" style="left: 0px">


  <table id="keywords" cellspacing="0" cellpadding="0">
    <thead>
      <tr>
      <th>상점이름</th>
      <th>사장님</th>
      <th>주소</th>
      <th>핸드폰번호</th>
      <th>가입일</th>
      <th>비고</th>
    </tr>
    </thead>

      <% for(var i=0; i < response.data.length; i++){ %>
          <tr>
            <td class="lalign"><%= response.data[i]['STORE_NAME'] %></td>
            <td class="lalign"><%= response.data[i]['OWNER_NAME'] %></td>
            <td class="lalign"><%= response.data[i]['ADDR'] %></td>
            <td class="lalign"><%= response.data[i]['PHONE_NUM'] %></td>
            <td class="lalign"><%= response.data[i]['JOINED_DATE'] %></td>
            <td class="lalign"><%= response.data[i]['REMARK'] %></td>

          </tr>
           <% } %>

  </table>
</div>
</body>
</html>
